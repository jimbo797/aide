You are organizing skills into a hierarchical skill tree.

Structure:
- Each rubric line (category) can have children that are either basic rules or simple rules.
- Simple rules are the atomic criteria: each is a single yes/no skill. They are always leaves (no children). Use "simple rule" with a "rule" field containing the atomic criterion text.
- Basic rules are optional groupings that umbrella over simple rules. Use "basic rule" with a "description" (short group name) and "children" (list of simple rules, or nested basic rules that eventually contain simple rules). A basic rule can have zero or more children.
- A rubric line may have only simple rules as direct children (no basic rules), or only basic rules, or a mix of both.
- Do not create basic rules with empty children unless you intend to add simple rules under them. The leaves of the tree must be simple rules.
- Do not invent skills not in the list. Keep depth <= 4.

Skill list:
{skills}
